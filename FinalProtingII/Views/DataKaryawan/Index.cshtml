@model IEnumerable<FinalProtingII.Models.Karyawan>
@{
    ViewBag.Title = "Data Karyawan";
    ViewBag.ActiveMenu = "DataKaryawan";
}

<div class="container-fluid">
    <div class="card card-primary card-outline">
        <div class="card-header bg-primary text-white">
            <h4 class="card-title mb-0">Search Karyawan</h4>
        </div>
        <div class="card-body" style="padding: 25px 25px 10px 25px">
            <div class="row">
                <div class="col-lg-6">
                    <div class="form-group row">
                        <label class="col-sm-4 col-form-label">ID Karyawan</label>
                        <div class="col-sm-8">
                            <select class="form-control" id="searchIDKaryawan"></select>
                        </div>
                    </div>
                </div>
                <div class="col-lg-6">
                    <div class="form-group row">
                        <label class="col-sm-4 col-form-label">Nama Karyawan</label>
                        <div class="col-sm-8">
                            <select class="form-control" id="searchKaryawanName"></select>
                        </div>
                    </div>
                </div>
            </div>
            <div class="row">
                <a asp-action="Create" class="btn btn-success mb-3"><i class="fas fa-plus"></i> Tambah Karyawan</a>
            </div>
        </div>
    </div>

    <h3>Data Karyawan</h3>

    <div class="card">
        <div class="card-body">
            <table class="table table-bordered table-striped">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Nama</th>
                        <th>Aksi</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var k in Model)
                    {
                        <tr>
                            <td>@k.Id</td>
                            <td>@k.Nama</td>
                            <td>
                                <a asp-action="Edit" asp-route-id="@k.Id" class="btn btn-primary btn-sm"><i class="fas fa-edit"></i></a>
                                <a asp-action="Delete" asp-route-id="@k.Id" class="btn btn-danger btn-sm"><i class="fas fa-trash"></i></a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>




